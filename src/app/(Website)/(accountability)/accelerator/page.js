import WebWrapper from "@/components/shared/WebWrapper/WebWrapper";

const Accelerator = () => {
    return (
        <WebWrapper>
            <div>Accelerator</div>
        </WebWrapper>
    );
};

export default Accelerator;
